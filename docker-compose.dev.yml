version: '3.9'

services:
  # ============================================
  # SaaS Manager API
  # ============================================
  manager:
    build:
      context: .
      dockerfile: apps/manager/Dockerfile
    container_name: manager
    ports:
      - '3000:3000'
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@saas-db:5432/apex_saas
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=dev-secret-key
      - VENDURE_URL=http://engine:3001/admin-api
      - STRIPE_SECRET_KEY=sk_test_placeholder_key_must_be_set_on_server
      - STRIPE_WEBHOOK_SECRET=whsec_placeholder_secret_must_be_set_on_server
      - STRIPE_CURRENCY=EGP
    depends_on:
      saas-db:
        condition: service_healthy
      redis:
        condition: service_healthy
      engine:
        condition: service_healthy
    restart: always

  # ============================================
  # Admin HQ (Next.js Frontend)
  # ============================================
  admin-hq:
    build:
      context: .
      dockerfile: apps/admin-hq/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://34.18.154.179:3000/api
    container_name: admin-hq
    ports:
      - '3003:3000' # Map internal 3000 to external 3003
    environment:
      - NEXT_PUBLIC_API_URL=http://34.18.154.179:3000/api
    restart: always

  # ============================================
  # Storefront (Next.js Customer App)
  # ============================================
  storefront:
    build:
      context: .
      dockerfile: apps/storefront/Dockerfile
      args:
        NEXT_PUBLIC_VENDURE_API_URL: http://34.18.154.179:3010/shop-api
        NEXT_PUBLIC_MANAGER_URL: http://34.18.154.179:3000
    container_name: storefront
    ports:
      - '3004:3000' # Map internal 3000 to external 3004 (to identify verify)
    environment:
      - NEXT_PUBLIC_VENDURE_API_URL=http://34.18.154.179:3010/shop-api
      - NEXT_PUBLIC_VENDURE_SHOP_API_URL=http://34.18.154.179:3010/shop-api
      - NEXT_PUBLIC_MANAGER_URL=http://34.18.154.179:3000
      - MANAGER_API_URL=http://manager:3000/api
    depends_on:
      manager:
        condition: service_started
      engine:
        condition: service_healthy
    restart: always

  # ============================================
  # Vendure Engine (E-commerce Core)
  # ============================================
  engine:
    build:
      context: .
      dockerfile: apps/engine/Dockerfile
    container_name: apex-engine
    ports:
      - '3010:3001' # Shop API (Mapped to 3010 to avoid conflict)
      - '3020:3002' # Admin UI (Mapped to 3020 to avoid conflict)
    environment:
      - DB_HOST=vendure-db
      - DB_PORT=5432
      - DB_NAME=vendure
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - REDIS_URL=redis://redis:6379
      - COOKIE_SECRET=dev-secret-key
    volumes:
      - ./apps/engine/static/assets:/app/apps/engine/static/assets
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3001/health" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s
    depends_on:
      vendure-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  # ============================================
  # SaaS Database (Prisma) - Manager API
  # ============================================
  saas-db:
    image: postgres:15-alpine
    container_name: apex-saas-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: apex_saas
    volumes:
      - saas_db_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # ============================================
  # Vendure Database - E-commerce Engine
  # ============================================
  vendure-db:
    image: postgres:15-alpine
    container_name: apex-vendure-db
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vendure
    volumes:
      - vendure_db_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # ============================================
  # Redis - Sessions & Cache
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: apex-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  saas_db_data:
  vendure_db_data:
  redis_data:
